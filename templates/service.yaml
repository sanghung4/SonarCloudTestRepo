apiVersion: v1
kind: Service
metadata:
  name: {{ include "punchout-customer-ui.fullname" . }}
  labels:
    {{- include "punchout-customer-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "punchout-customer-ui.selectorLabels" . | nindent 4 }}
