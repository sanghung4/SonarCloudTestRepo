package com.reece.pickingapp.wrappers

import android.content.Context
//import com.okta.oidc.OIDCConfig

class OIDCConfigWrapper {
    //lateinit var config: OIDCConfig

    fun createConfig(context: Context, rawResId: Int) {
        /*config = OIDCConfig.Builder()
            .withJsonFile(context, rawResId)
            .create()*/
    }
}