package com.reece.pickingapp.utils

enum class SnackBarType {
    SUCCESS,
    ERROR,
    MESSAGE,
    WARNING
}

enum class DataEntryFieldType {
    DEFAULT,
    SCANNABLE_PRODUCT,
    SCANNABLE_SERIAL,
    PASSWORD,
    NUMBER_ENTRY,
    QUANTITY_NUMBER
}

enum class PackageType {
    BUND,
    BOX,
    SKID,
    PCS
}