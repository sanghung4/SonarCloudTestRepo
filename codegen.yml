overwrite: true
schema: 
  "https://api.internal-test.reecedev.us"
documents: "src/graphql/*.graphql"
generates:
  src/graphql/index.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    hooks:
      afterOneFileWrite:
        - prettier --write
