overwrite: true
schema: 
  - http://localhost:4000:
      headers: 
        x-max-api-secret:  ${REACT_APP_MAX_API_SECRET:DFhdhdTolx}
documents: "src/**/*.graphql"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      withHooks: true
