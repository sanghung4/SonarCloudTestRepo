import { SelectedAccounts } from 'providers/SelectedAccountsProvider';

export const mockSelectedAccounts: SelectedAccounts = {
  erpSystemName: undefined,
  shipTo: { accountNumber: undefined, accountName: '' }
};
