mutation AddToCount($item: ItemInput!) {
  addToCount(item: $item) {
    success
    message
    item {
      id
      prodDesc
      prodNum
      catalogNum
      tagNum
      uom
      quantity
      sequence
      productImageUrl
    }
  }
}
