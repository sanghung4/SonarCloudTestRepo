import { Typography, styled } from '@dialexa/reece-component-library';

export const ItemsOrderedTypography = styled(Typography)(({ theme }) => ({
  fontWeight: 400,
  fontSize: theme.spacing(1.5)
}));
