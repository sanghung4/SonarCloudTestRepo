apiVersion: v1
kind: Service
metadata:
  name: {{ include "internal-api.fullname" . }}
  labels:
    {{- include "internal-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "internal-api.selectorLabels" . | nindent 4 }}
