apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "internal-api.fullname" . }}-test-connection"
  labels:
    {{- include "internal-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "internal-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
