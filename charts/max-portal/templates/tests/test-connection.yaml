apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "max-portal.fullname" . }}-test-connection"
  labels:
    {{- include "max-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "max-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
