apiVersion: v1
kind: Service
metadata:
  name: {{ include "max-portal.fullname" . }}
  labels:
    {{- include "max-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "max-portal.selectorLabels" . | nindent 4 }}
