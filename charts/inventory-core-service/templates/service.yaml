apiVersion: v1
kind: Service
metadata:
  name: {{ include "inventory-core-service.fullname" . }}
  labels:
    {{- include "inventory-core-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inventory-core-service.selectorLabels" . | nindent 4 }}
