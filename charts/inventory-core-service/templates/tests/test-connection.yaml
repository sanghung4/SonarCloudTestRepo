apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inventory-core-service.fullname" . }}-test-connection"
  labels:
    {{- include "inventory-core-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inventory-core-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
