apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "punchout-customer-bff.fullname" . }}-test-connection"
  labels:
    {{- include "punchout-customer-bff.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "punchout-customer-bff.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
