apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "configuration-service.fullname" . }}-test-connection"
  labels:
    {{- include "configuration-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "configuration-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
