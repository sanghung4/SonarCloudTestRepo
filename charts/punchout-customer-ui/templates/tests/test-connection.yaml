apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "punchout-customer-ui.fullname" . }}-test-connection"
  labels:
    {{- include "punchout-customer-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "punchout-customer-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
