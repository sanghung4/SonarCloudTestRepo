apiVersion: v1
kind: Service
metadata:
  name: {{ include "products-core-service.fullname" . }}
  labels:
    {{- include "products-core-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "products-core-service.selectorLabels" . | nindent 4 }}
