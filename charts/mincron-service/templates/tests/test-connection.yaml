apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mincron-service.fullname" . }}-test-connection"
  labels:
    {{- include "mincron-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mincron-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
