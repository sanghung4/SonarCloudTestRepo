apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "picking-core-service.fullname" . }}-test-connection"
  labels:
    {{- include "picking-core-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "picking-core-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
