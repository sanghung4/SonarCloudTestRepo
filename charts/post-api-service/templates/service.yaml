apiVersion: v1
kind: Service
metadata:
  name: {{ include "post-api-service.fullname" . }}
  labels:
    {{- include "post-api-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "post-api-service.selectorLabels" . | nindent 4 }}
