apiVersion: v1
kind: Service
metadata:
  name: {{ include "wms-mincron-service.fullname" . }}
  labels:
    {{- include "wms-mincron-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wms-mincron-service.selectorLabels" . | nindent 4 }}
