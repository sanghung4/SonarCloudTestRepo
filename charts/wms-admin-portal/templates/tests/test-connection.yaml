apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wms-admin-portal.fullname" . }}-test-connection"
  labels:
    {{- include "wms-admin-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wms-admin-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
