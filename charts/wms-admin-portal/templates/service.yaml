apiVersion: v1
kind: Service
metadata:
  name: {{ include "wms-admin-portal.fullname" . }}
  labels:
    {{- include "wms-admin-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wms-admin-portal.selectorLabels" . | nindent 4 }}
