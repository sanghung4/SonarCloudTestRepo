apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "special-pricing-service.fullname" . }}-test-connection"
  labels:
    {{- include "special-pricing-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "special-pricing-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never