apiVersion: v1
kind: Service
metadata:
  name: {{ include "special-pricing-service.fullname" . }}
  labels:
    {{- include "special-pricing-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "special-pricing-service.selectorLabels" . | nindent 4 }}
