apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "punchout-customer-catalog.fullname" . }}-test-connection"
  labels:
    {{- include "punchout-customer-catalog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "punchout-customer-catalog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
