apiVersion: v1
kind: Service
metadata:
  name: {{ include "punchout-customer-catalog.fullname" . }}
  labels:
    {{- include "punchout-customer-catalog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "punchout-customer-catalog.selectorLabels" . | nindent 4 }}
