apiVersion: v1
kind: Service
metadata:
  name: {{ include "account-management-service.fullname" . }}
  labels:
    {{- include "account-management-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "account-management-service.selectorLabels" . | nindent 4 }}
