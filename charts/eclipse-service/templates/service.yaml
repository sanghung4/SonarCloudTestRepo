apiVersion: v1
kind: Service
metadata:
  name: {{ include "eclipse-service.fullname" . }}
  labels:
    {{- include "eclipse-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eclipse-service.selectorLabels" . | nindent 4 }}
