apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eclipse-service.fullname" . }}-test-connection"
  labels:
    {{- include "eclipse-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eclipse-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
