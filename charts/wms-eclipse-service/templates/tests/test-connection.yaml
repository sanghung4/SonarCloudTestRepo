apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wms-eclipse-service.fullname" . }}-test-connection"
  labels:
    {{- include "wms-eclipse-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wms-eclipse-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
